# 部署踩坑记录

本文档记录本次 `OutBaoBao` 从本地到线上部署过程中实际遇到的问题、根因和对应处理方式，后续再次部署时可直接对照排查。

## 1. Node / pnpm 版本不匹配

### 现象

执行：

```bash
pnpm prisma db push
```

报错类似：

```text
This version of pnpm requires at least Node.js v22.13
Error [ERR_UNKNOWN_BUILTIN_MODULE]: No such built-in module: node:sqlite
```

### 根因

- 服务器安装的是 `Node 20`
- 项目锁定的是 `pnpm 11.1.1`
- `pnpm 11` 依赖更高版本 Node

### 解决

升级到 `Node 22`，然后重新启用 pnpm：

```bash
sudo apt remove -y nodejs
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs

sudo npm install -g pnpm@11.1.1
```

## 2. `corepack enable` 权限不足

### 现象

执行：

```bash
corepack enable
```

报错：

```text
EACCES: permission denied, symlink ... -> /usr/bin/yarn
```

### 根因

- 当前用户无权写 `/usr/bin`

### 解决

两种方式：

```bash
sudo corepack enable
sudo corepack prepare pnpm@11.1.1 --activate
```

或直接：

```bash
sudo npm install -g pnpm@11.1.1
```

本次采用的是第二种。

## 3. SQLite 数据库文件无法创建

### 现象

执行：

```bash
pnpm prisma db push
```

报错：

```text
unable to open database file: /var/www/outbaobao/data/prod.db
```

### 根因

- 数据目录不存在
- 或当前用户对目录无写权限

### 解决

```bash
mkdir -p /var/www/outbaobao/data
mkdir -p /var/www/outbaobao/public/uploads
sudo chown -R $(whoami):$(whoami) /var/www/outbaobao
```

## 4. PM2 启动命令写错

### 现象

PM2 一直 `errored`，日志里报：

```text
Invalid project directory provided, no such directory: .../--hostname
```

### 根因

用了错误命令：

```bash
pnpm start -- --hostname 127.0.0.1 --port 3000
```

这里的 `--` 会让 `next start` 把后面的参数误判成项目目录。

### 解决

正确命令：

```bash
pnpm start --hostname 127.0.0.1 --port 3000
```

PM2 启动方式：

```bash
pm2 delete outbaobao
pm2 start "pnpm start --hostname 127.0.0.1 --port 3000" --name outbaobao
pm2 save
```

## 5. 未先 build 就 start

### 现象

执行：

```bash
pnpm start --hostname 127.0.0.1 --port 3000
```

报错：

```text
Could not find a production build in the '.next' directory
```

### 根因

- 还没有执行 `pnpm build`

### 解决

先执行：

```bash
pnpm build
```

再执行：

```bash
pnpm start --hostname 127.0.0.1 --port 3000
```

## 6. 后台登录看似失效，实际是主机名不一致

### 现象

- 在 `localhost` 打开后台登录
- 登录后又回到登录页

### 根因

- 登录 cookie 写在 `localhost`
- 服务端跳转到了 `127.0.0.1`
- 两个主机名不同，cookie 不共享

### 解决

- 开发环境统一使用同一个主机名
- 代码层修复：登录/退出跳转基于当前请求域名，不硬编码走 `SITE_URL`

本次已修复代码。

## 7. `.env.production` 可能带 Windows 回车

### 现象

- `pm2 env 0` 看起来密码对
- 但登录始终提示密码错误

### 根因

- `.env.production` 从 Windows 带上来时是 `CRLF`
- 变量实际可能是 `jss6667\r`

### 解决

统一转成 Unix 行尾：

```bash
sed -i 's/\r$//' .env.production
```

然后重新加载环境并重启 PM2。

## 8. 域名 `serverHold`，无法解析

### 现象

```bash
ping jss309309.com
```

返回：

```text
No address associated with hostname
```

### 根因

- 域名注册局还未解除 `serverHold`
- DNS 解析尚未生效

### 解决

- 等待注册局状态恢复
- 恢复后再配置 A 记录和证书

## 9. Nginx 配置正确，但公网访问卡住

### 现象

- `curl http://127.0.0.1:3000` 正常
- `curl http://127.0.0.1 -H "Host: 域名"` 正常
- `curl http://域名` 卡住

### 根因

- 应用本身没问题
- Nginx 反代也没问题
- 剩下通常就是云安全组未放行 `80/443`

### 解决

在云控制台放行：

- TCP 80
- TCP 443
- TCP 22

## 10. 图片上传成功，但访问 `/uploads/...` 返回 404

### 现象

- 后台上传接口成功
- `public/uploads` 里也有对应文件
- 但浏览器访问图片返回 404

### 根因

- 运行时新上传的文件不适合继续完全依赖 Next 处理
- 生产环境更稳的方案应该让 Nginx 直接托管上传目录

### 解决

在 Nginx 中增加：

```nginx
location /uploads/ {
    alias /home/ubuntu/var/www/outbaobao/public/uploads/;
    access_log off;
    expires 30d;
    add_header Cache-Control "public, max-age=2592000";
}
```

## 11. 图片返回 403

### 现象

访问：

```text
https://域名/uploads/xxx.png
```

返回：

```text
403 Forbidden
```

### 根因

- Nginx 已经命中 `/uploads/`
- 但 `www-data` 无法穿透 `/home/ubuntu/...` 目录层级读取文件

### 解决

为路径补执行权限、为文件补读权限：

```bash
sudo chmod o+x /home
sudo chmod o+x /home/ubuntu
sudo chmod o+x /home/ubuntu/var
sudo chmod o+x /home/ubuntu/var/www
sudo chmod o+x /home/ubuntu/var/www/outbaobao
sudo chmod o+x /home/ubuntu/var/www/outbaobao/public
sudo chmod 755 /home/ubuntu/var/www/outbaobao/public/uploads
sudo find /home/ubuntu/var/www/outbaobao/public/uploads -type f -exec chmod 644 {} \;
```

## 12. 图片加载慢

### 现象

- 图片能打开
- 但加载明显慢，尤其手机和微信中更明显

### 根因

- 使用的是香港服务器
- 带宽只有 `3Mbps`
- 文章图按原图直出
- 单张图片可能几 MB

### 结论

- 慢的主因是带宽和原图体积，不是 CPU/内存

### 建议

优先级：

1. 升级带宽
2. 上 CDN
3. 长期考虑对象存储

## 13. 微信分享卡片文字正常、图片是破图

### 现象

- `og:title`、`og:description` 正常
- 微信分享图显示默认未加载图标

### 根因

线上文章页实际输出的是：

```html
<meta property="og:image" content="http://localhost:3000/uploads/xxx.jpg">
```

而不是正式线上域名。

微信能抓文字，但抓不到这个 `localhost` 图片。

### 解决

代码层已经修复：

- 增加统一站点 URL 辅助
- 分享图统一转绝对地址
- 设置 `metadataBase`

修复后应输出：

```html
<meta property="og:image" content="https://jss309309.com/uploads/xxx.jpg">
```

## 14. 未登录访问 `/admin` 跳到 `localhost/admin/login`

### 现象

线上直接访问：

```text
https://jss309309.com/admin
```

可能跳到：

```text
http://localhost:3000/admin/login
```

### 根因

- 登录/退出接口原先使用 `SITE_URL` 或默认回退值
- 部分情况下实际请求域名与配置值不一致

### 解决

代码层已经修复：

- 登录/退出跳转统一基于当前请求 `request.url` 的 origin
- 不再硬跳 `localhost`

## 15. 项目目录路径混乱

### 现象

部署过程中出现过两套路径混用：

- `/var/www/outbaobao`
- `/home/ubuntu/var/www/outbaobao`

### 风险

- 数据库路径错
- 图片目录错
- PM2 工作目录错
- Nginx `alias` 指错

### 本次最终确认使用的目录

```text
/home/ubuntu/var/www/outbaobao
```

后续所有配置都应统一按这个目录走。

## 16. 本次最终线上关键配置

### 应用目录

```text
/home/ubuntu/var/www/outbaobao
```

### 数据库

```text
/home/ubuntu/var/www/outbaobao/data/prod.db
```

### 上传图片目录

```text
/home/ubuntu/var/www/outbaobao/public/uploads
```

### 正式域名

```text
https://jss309309.com
```

### PM2 启动命令

```bash
pm2 start "pnpm start --hostname 127.0.0.1 --port 3000" --name outbaobao
```

## 17. 推荐的标准部署顺序

```bash
git clone ...
cd /home/ubuntu/var/www/outbaobao
pnpm install --frozen-lockfile
sed -i 's/\r$//' .env.production
set -a
source .env.production
set +a
pnpm prisma db push
pnpm build
pm2 start "pnpm start --hostname 127.0.0.1 --port 3000" --name outbaobao
pm2 save
sudo nginx -t
sudo systemctl reload nginx
sudo certbot --nginx -d jss309309.com -d www.jss309309.com
```

## 18. 推荐上线后检查项

### 应用状态

```bash
pm2 status
pm2 logs outbaobao --lines 50 --nostream
```

### 本机访问

```bash
curl -I http://127.0.0.1:3000
curl -I http://127.0.0.1 -H "Host: jss309309.com"
```

### 域名访问

```bash
curl -I http://jss309309.com
curl -I https://jss309309.com
```

### 图片访问

```bash
curl -I https://jss309309.com/uploads/某张图片文件名
```

### 登录跳转

- 未登录访问 `/admin` 应跳 `/admin/login`
- 登录后应跳 `/admin`

### 分享元数据

查看页面源码，确认：

- `og:title`
- `og:description`
- `og:image`

其中 `og:image` 必须是正式绝对地址。
